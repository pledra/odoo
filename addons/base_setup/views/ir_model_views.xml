<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="model_form_view" model="ir.ui.view">
        <field name="model">ir.model</field>
        <field name="inherit_id" ref="base.view_model_form"/>
        <field name="arch" type="xml">
            <field name="transient" position="after">
                <field name="is_setting_model" readonly="1" groups="base.group_no_one"/>
            </field>
        </field>
    </record>

    <record id="model_search_view" model="ir.ui.view">
        <field name="model">ir.model</field>
        <field name="inherit_id" ref="base.view_model_search"/>
        <field name="arch" type="xml">
            <field name="model" position="after">
                <filter string="Setting Models" name="is_setting_model" domain="[('is_setting_model', '=', True)]"/>
            </field>
        </field>
    </record>

</odoo>