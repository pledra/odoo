<?xml version='1.0' encoding='utf-8'?>
<odoo>

  <record id="digest_view_tree" model="ir.ui.view">
    <field name="name">digest.tree</field>
    <field name="model">digest.digest</field>
    <field name="arch" type="xml">
      <tree string="KPI Digest">
        <field name="name"/>
        <field name="periodicity"/>
        <field name="next_run_date" groups="base.group_no_one"/>
        <field name="active"/>
      </tree>
    </field>
  </record>

  <record id="digest_view_form" model="ir.ui.view">
    <field name="name">digest.form</field>
    <field name="model">digest.digest</field>
    <field name="arch" type="xml">
      <form string="KPI Digest">
        <header>
          <button type="object" name="send_digests_now" string="Send digest now" class="oe_highlight"/>
        </header>
        <sheet>
          <div class="oe_button_box" name="button_box">
            <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-check">
              <field name="active" widget="boolean_button" options="{'terminology': 'active'}"/>
            </button>
          </div>
          <div class="oe_title">
            <label for="name" class="oe_edit_only"/>
            <h1>
              <field name="name"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="periodicity" widget="radio" options="{'horizontal': true}"/>
              <field name="user_ids" widget="many2many_tags" options="{'no_create': True}"/>
              <field name="template_id" groups="base.group_no_one"/>
              <field name="next_run_date" groups="base.group_no_one"/>
              <field name='company_id' options="{'no_create': True}" groups="base.group_multi_company" />
            </group>
          </group>
          <notebook>
            <page name="kpis" string="KPIs">
              <group name="kpis">
                <group name="kpi_general" string="General">
                  <field name="kpi_res_users_connected"/>
                  <field name="kpi_mail_message_total"/>
                </group>
              </group>
            </page>
            <page name="how_to" string="How to customize your digest?" groups="base.group_no_one">
              <div class="alert alert-info">
                In order to build your customized digest, follow these steps:
                <ol>
                  <li>You may want to add new computed fields with Odoo Studio:
                    <ul>
                      <li>you must create 2 fields on the <code>digest</code> object:</li>
                      <li>first create a boolean field called <code>x_kpi_myfield</code> and display it in the KPI's tab;</li>
                      <li>then create a computed field called <code>x_kpi_myfield_value</code> that will compute your customized KPI.</li>
                    </ul>
                  </li>
                  <li>Select your KPIs in the KPI's tab.</li>
                  <li>Create or edit the mail template: you may get computed KPI's value using these fields: <code><field name="available_fields" class='oe_inline'/></code></li>
                </ol>
              </div>
            </page>
          </notebook>
        </sheet>
      </form>
    </field>
  </record>

  <record id="digest_view_search" model="ir.ui.view">
    <field name="name">digest.search</field>
    <field name="model">digest.digest</field>
    <field name="arch" type="xml">
      <search>
        <field name="name"/>
        <field name="user_ids"/>
        <filter string="Inactive" name="inactive" domain="[('active', '=', False)]"/>
        <group expand="1" string="Group by">
          <filter string="Periodicity" name='periodicity' context="{'group_by': 'periodicity'}"/>
        </group>
      </search>
    </field>
  </record>

  <record id="digest_action" model="ir.actions.act_window">
    <field name="name">KPI Digests</field>
    <field name="res_model">digest.digest</field>
    <field name="view_type">form</field>
    <field name="search_view_id" ref="digest_view_search"/>
  </record>

  <menuitem id="digest_menu" parent="base.menu_email" sequence="93" action="digest_action"/>
</odoo>
